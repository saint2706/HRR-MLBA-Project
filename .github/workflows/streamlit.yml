name: keep-streamlit-awake
on:
  schedule:
    - cron: "0 */2 * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit app URL
        run: curl -sS https://hrr-mlba.streamlit.app/ > /dev/null || true 
